- 11장. pptx 모듈 사용시 에러 발생 문제

- 명령 프롬프트에서 아래 명령으로 python-pptx 를 1.0.2 버전으로 설치합니다.
pip install python-pptx==1.0.2

- python-pptx 라이브러리(패키지)의 버전이 1.0.2 이전 버전(낮은 버전)을 사용해야 하는 경우에는,
  아래 2줄의 코드를 pptx 모듈을 사용하는 코드의 맨 첫번째 2줄에 넣으세요.
	import collections 
	import collections.abc

예)
import collections 
import collections.abc
# 추가 import 문
from pptx import Presentation
#....

에러 메시지 참고)
AttributeError: module 'collections' has no attribute 'Container'
in File "...\lib\site-packages\pptx_init_.py",


